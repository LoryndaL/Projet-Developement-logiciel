name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # Ce workflow s'exécute lorsque des changements sont poussés sur la branche 'main'
  pull_request:
    branches:
      - main  # Ce workflow s'exécute lorsqu'un pull request est fait vers la branche 'main'

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9  # Définir la version de Python

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Installer les dépendances (si tu as un fichier requirements.txt)

      - name: Run flake8
        run: |
          pip install flake8
          flake8 .  # Lancer flake8 pour le linting

  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run black
        run: |
          pip install black
          black --check .  # Vérifier si le formatage du code est correct avec black

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest
        run: |
          pip install pytest
          pytest  # Exécuter les tests avec pytest
